import tkinter as tk    # abbreviate the term

root = tk.Tk()  # create a window(object)
root.title('Test')  # default title for window is tk, if not setting the title
root.mainloop() # method to keep monitor the movement inside the window (check activity in the window)
